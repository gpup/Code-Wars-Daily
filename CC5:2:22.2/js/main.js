
// Nice easy one to bounce back from the last one

function greet (name, owner) {
    // if else to return the two messages
    if(name===owner){
      return 'Hello boss'
    }else{
      return 'Hello guest'
    }
  };