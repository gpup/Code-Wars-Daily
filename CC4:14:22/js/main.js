const arr = N => [ /* the numbers 0 to N-1 */ ];

// Organize elements of array in ascending order
arr.sort();
// It should return N-1, or up to greatest number in array set

function arr(N){
    let arr = [];
    for(let i =0; i < N; i++){
        arr.push(i)
    }
    return arr;
}